# 更新日志

本文件记录 AllSessions 各版本面向用户的主要变化。

## [未发布]

## [0.0.23] - 2026-09-01

### 新增

- 增加像素工作站主题，提供浅色、深色、高对比选中态和像素滚动条。
- 为工具页增加侧边栏导航与工具面板切换。

### 变更

- 在更新安装确认中展示对应版本的更新日志。
- 稳定页面滚动条占位，减少工作区切换时的布局抖动。

## [0.0.22] - 2026-08-31

### 新增

- 在设置中支持手动输入或通过系统选择器添加会话来源目录和数据库。

### 变更

- 优化工作区导航、统计页默认展开状态和宽屏布局。
- 将会话归档操作移至详情页，并在归档前增加二次确认。
- 调整界面主题名称，提升含义辨识度。

## [0.0.21] - 2026-08-28

### 变更

- 优化顶部工作区布局，将来源栏开关收纳为图标按钮，并统一导航、搜索、状态和操作控件的高度。
- 优化窄窗口下的会话列表信息层级、批量选择操作和触控区域。
- 日期分组内的会话仅显示时间，减少重复日期信息。

## [0.0.20] - 2026-08-27

### 新增

- 增加 OpenCode 最新正式版 SQLite 会话来源，支持消息、Thinking、工具、子 Agent、搜索、原始事件与实时刷新；来源数据库保持只读。
- 统计页增加按 Agent 的会话、消息、工具、活跃度、Provider 和工作目录概览。
- 设置中增加五套界面主题，以及浅色、深色和跟随系统模式。

### 变更

- 重构多来源工作台的信息层级、导航与设置界面，并补充键盘导航。
- 更新中英文公开文档，使支持范围、界面能力和浏览器预览方式与当前版本一致。

## [0.0.19] - 2026-08-26

### 新增

- 增加 Pi 与 Kimi Code CLI 本地会话来源，支持扫描、搜索、实时刷新、工具记录、原始事件和设置管理。

### 修复

- 对齐 Kimi Code CLI 官方 Wire 内容与工具结果格式，并正确关联工具名称和错误状态。
- 从 Pi 父会话文件头读取真实会话标识，并兼容 Pi、Kimi Code CLI 的官方数据目录环境变量。
- 开发构建不再于启动时连接正式更新通道，避免本地调试触发更新提示。

### 变更

- Pi 与 Kimi Code CLI 来源暂时保持只读；可在 AllSessions 中本地移除记录，但原始数据需在对应 Agent 中删除。

## [0.0.18] - 2026-08-24

### 修复

- 会话列表、日期分组、筛选和统计统一按最近活动时间计算，确保当天仍有新消息的会话显示在正确日期。

### 变更

- 消息角色、工具调用和系统上下文筛选项改为常驻显示，减少查看会话时的额外操作。
- 拆分 Codex 维护视图并将维护状态计算纯函数化，降低维护逻辑复杂度且保持现有功能不变。

## [0.0.17] - 2026-08-24

### 新增

- 设置中的会话来源增加扫描健康状态、文件监听状态和可复制的脱敏诊断信息。
- 永久删除会话或消息前自动创建最小本地备份，并在存储设置和删除结果中显示备份位置。
- 增加独立持久化的个人工作台，可为会话设置收藏、标签、备注、本地归档与移除状态，并保存常用筛选条件。
- 支持批量导出当前选择的会话，可选脱敏本地路径和会话标识，并可在系统文件管理器中显示来源文件或项目目录。

### 修复

- 配置文件损坏时改用安全默认值启动，并自动进入来源设置，避免应用因无法读取配置而完全不可用。
- 单个来源文件解析失败时保留其他可用来源和会话，同时记录来源级错误用于诊断。
- 完整脱敏包含空格的本地路径，并限制批量导出的数量和并发，避免大批量详情读取造成资源峰值。
- 收紧工作台 SQLite 主库、WAL 与共享内存文件权限，避免备注、标签等本地数据通过日志侧文件暴露。

### 变更

- 更新安全边界、README 和反馈模板，使其与纯 Tauri IPC、本地敏感数据和当前支持来源保持一致。
- 关于页面增加可安全打开的许可证详情链接，并明确区分 Agent 原始记录与 AllSessions 用户数据。

## [0.0.16] - 2026-08-22

### 新增

- 设置中心新增关闭窗口后继续运行和启动时检查更新选项，并在系统托盘中提供快速打开设置的入口。
- 关于页面的项目仓库地址支持通过系统默认浏览器安全打开。

### 修复

- 启动时自动检查更新在已是最新版本时不再弹窗，手动检查更新也不会关闭当前设置窗口。
- 修复发布流程中草稿更新清单地址和重复安装包上传导致的构建、发布失败问题。

### 变更

- 重新整理设置窗口的关于、会话来源和存储页面，使来源状态、扫描路径与缓存操作更紧凑清晰。
- 顶栏搜索快捷键根据当前平台显示，并优化会话数量在窄窗口中的排版。
- 增加独立的浏览器预览开发命令，便于在不启动桌面构建的情况下调试界面。

## [0.0.15] - 2026-08-21

### 新增

- 支持将单个会话或消息仅从 AllSessions 移除并随时恢复，也可在二次确认后永久删除 Codex、Claude Code 和 Gemini CLI 的对应原始数据。
- 设置页区分受保护的默认来源与附加目录，允许显式停用来源，同时避免误删应用继承的扫描路径。

### 修复

- 永久删除共享日志中的内容时保留其他会话，并校验原始记录变化、工具调用关联和 Gemini 目录边界，降低误删风险。
- 删除确认窗口改用本地化关闭控件，修复中英文混用问题。

### 变更

- 使用透明、高分辨率的 v3 品牌图标，统一网页、macOS、Windows、应用窗口与菜单栏图标资产。

## [0.0.14] - 2026-08-19

### 新增

- 顶栏新增「设置」对话框：可切换界面语言、按来源自定义会话根目录列表（保存后立即重建索引并更新文件监听，优先级高于环境变量）、查看并清除索引缓存，以及查看版本信息。配置持久化于系统用户配置目录下的 `AllSessions/config.json`，可用 `ALLSESSIONS_CONFIG_PATH` 覆盖。

### 修复

- Codex Provider 修复流程改为明确的四步操作，并在执行成功后进入完成态，避免再次生成包含旧 Provider 的失效计划。
- 将修复回滚拆为独立工具页面，隔离维护模式、退出确认与执行状态，减少误操作。

### 变更

- 会话首页在宽屏下改为“会话列表、会话内容、会话信息”三栏布局；中等宽度回退为信息抽屉，移动端保持单栏操作。
- 精简顶部工具栏，将筛选入口统一归入左侧会话栏，顶部仅保留全局导航、搜索与应用级操作。

## [0.0.13] - 2026-08-17

### 修复

- 应用窗口、任务栏与安装包图标去除方形底色，并为小尺寸补充锐化和更多 DPI 档位，缓解 Windows 任务栏图标模糊。

### 变更

- Codex Provider 修复流程增加“下一步”提示、分阶段按钮文案和旧计划过期标记，并明确执行与回滚共享的安全确认。
- 宽屏（窗口 ≥1600px）下会话阅读列放宽至 1200px，整体页面宽度上限提升至 2240px，减少最大化时两侧空白。

## [0.0.12] - 2026-08-16

### 新增

- `CODEX_SESSIONS_DIR`、`CODEX_ARCHIVED_SESSIONS_DIR`、`CLAUDE_SESSIONS_DIR`、`GEMINI_SESSIONS_DIR` 支持以系统路径分隔符分隔的多个根目录，并支持前导 `~` 与 Windows `~\` 展开；同一物理文件和同一会话在多个根中重复时只保留首个声明根中的副本。Codex Provider 维护工具仍只覆盖主 `CODEX_HOME` 目录。
- Gemini CLI 日志改为流式扫描、逐文件增量缓存、跨文件会话聚合和有界按需详情；brain artifact 会与日志去重，且缓存会随 artifact 提示变化自动失效。
- CI 覆盖 Windows、macOS 与 Ubuntu，并将 Rust 测试代码纳入 Clippy 门禁。

### 变更

- 拆分会话详情过滤/渲染、消息导航和统计视图模块，降低主应用模块体积。
- 第三方许可证清单校验忽略 Windows 与 Unix 换行差异，并补充 Markdown 安全渲染、会话视图和文本格式回归测试。

## [0.0.11] - 2026-08-14

### 修复

- 更新确认框使用与应用一致的中文操作按钮，避免原生窗口出现英文 `Yes/No`。
- 托盘图标改为透明画布；macOS 菜单栏使用模板图标，避免显示方形白底。

## [0.0.10] - 2026-08-14

### 新增

- 首次读取本地会话失败时可在工作区内重试；非桌面环境会给出明确引导。

### 修复

- 修复状态变更播报被视觉隐藏样式移出无障碍树的问题，并稳定识别桌面运行时缺失。

### 变更

- 简化来源筛选和详情工具栏，优化会话卡片信息层级、窄屏布局及键盘操作。

## [0.0.9] - 2026-08-13

### 修复

- 修复跨平台更新清单缺少回退平台时导致检查更新失败的问题。
- 统一桌面应用显示版本与 macOS 构建版本，避免版本号重复显示。

### 变更

- 将会话解析、缓存、文件监听和维护功能迁移到 Tauri Rust 进程，移除 Node.js sidecar。
- 更新前端通信、安装包构建与发布流程，使桌面运行时和更新流程保持一致。

## [0.0.8] - 2026-08-13

### 修复

- 修复 Windows 桌面版启动后同时显示控制台窗口的问题。

### 变更

- 增加 push、Pull Request 和发布前质量门禁，统一验证许可证、测试、lint、Web 构建与 Tauri 编译。
- 固定桌面发行使用的 Node.js 版本，并随安装包提供 Node.js 完整许可证和 Rust 依赖清单。
- 将来源发现、解析、详情与刷新收敛到来源适配器，前端来源筛选改为动态生成。
- Gemini 改用持久化日志分片缓存；文件变化时只重读变化文件并重建受影响会话。
- 拆分前端基础样式、会话格式化与导出模块，并将 Claude Code 接入计划归档为正式来源说明。
- 为本地 HTTP 服务增加 CSP 与常用安全响应头；桌面端只连接当前进程使用随机令牌启动的服务实例。
- 桌面端改用动态 loopback 端口启动内置服务，避免固定 3210 端口冲突导致启动超时。

## [0.0.7] - 2026-08-12

### 变更

- 检查、签名校验、下载和安装更新改用 Tauri 官方 updater，移除自维护的跨平台下载器。
- 发布流程改用 Tauri Action 自动生成签名更新包和 `latest.json`，同时保留兼容旧版更新器的安装包名称。

## [0.0.6] - 2026-08-12

### 新增

- 使用 Tauri 2 提供 Windows、macOS 与 Linux 共用的原生窗口、系统托盘和更新安装能力。

### 变更

- 桌面版在应用窗口内展示会话，不再依赖系统浏览器。
- 发布流程改用 Tauri 官方打包器；macOS 安装包改为标准 `.dmg`。
- 保留现有 Node.js 服务作为内置 sidecar，用户无需额外安装 Node.js。

## [0.0.5] - 2026-08-12

### 新增

- Windows 托盘支持检查 GitHub Release 更新，确认后可自动下载、校验并启动安装程序。
- 更新包会校验 Release 记录的文件大小、Windows 可执行文件头及可用的 SHA-256 摘要。
- macOS 增加原生菜单栏启动器，Linux 增加 AppIndicator 托盘；三个平台均可检查、下载并启动对应安装程序。

### 修复

- 修复长工具命令和代码块撑宽会话详情、导致页面右侧溢出的问题。

### 变更

- macOS 安装包名称由 `darwin` 调整为更直观的 `mac`，例如 `AllSessions-0.0.5-mac-arm64.pkg`。

## [0.0.4] - 2026-08-12

### 新增

- Windows 使用带应用图标的原生托盘启动器，可打开本地查看器或退出后台服务。

## [0.0.3] - 2026-08-12

### 修复

- 改进 Windows 安装后的启动流程，等待本地服务就绪后再可靠打开查看器。

## [0.0.2] - 2026-08-12

### 修复

- 修复 Windows 安装后通过脚本启动时报“不是有效的 Win32 应用程序”的问题。

## [0.0.1] - 2026-08-11

### 新增

- 提供 Codex、Claude Code 和 Gemini CLI 本地会话浏览能力。
- 增加 Windows x64、macOS ARM64、macOS x64 和 Linux x64 安装包的自动构建与发布流程。
