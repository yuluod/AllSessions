{
  "name": "codex-session-viewer",
  "version": "1.0.0",
  "description": "A lightweight, local-only web viewer for browsing AI session history, currently focused on Codex sessions.",
  "type": "module",
  "license": "Apache-2.0",
  "packageManager": "pnpm@10.0.0",
  "keywords": ["ai", "codex", "session", "viewer", "local", "cli", "claude"],
  "scripts": {
    "start": "node server/index.js",
    "dev": "node server/index.js",
    "test": "node --test",
    "lint": "eslint server public --fix",
    "format": "prettier --write .",
    "build": "vite build"
  },
  "devDependencies": {
    "@eslint/js": "^9.25.0",
    "eslint": "^9.25.0",
    "globals": "^16.0.0",
    "prettier": "^3.5.0",
    "vite": "^8.0.0"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=10"
  }
}
